# Правки часть третья

Ты не правильно используешь ручку /finduser
Вот как её можно вызывать

curl -vX GET http://localhost:8080/finduser \
-H "Content-Type: application/json" \
-H "Authorization: Bearer $JWT_TOKEN" \
-d '{
  "role": "Автор"
}'

curl -vX GET http://localhost:8080/finduser \
-H "Content-Type: application/json" \
-H "Authorization: Bearer $JWT_TOKEN" \
-d '{
  "last_name": "Первый"
}'

curl -vX GET http://localhost:8080/finduser \
-H "Content-Type: application/json" \
-H "Authorization: Bearer $JWT_TOKEN" \
-d '{
  "first_name": "Автор"
}'

curl -vX GET http://localhost:8080/finduser \
-H "Content-Type: application/json" \
-H "Authorization: Bearer $JWT_TOKEN" \
-d '{
  "email": "author@example.com"
}'

Страницы Инстуркция и О проекте сделай в том же стиле, и что бы была навигационная панель слева

Есть страницы котоыре выдают белый экран, например вот эта верисия, у неё нету правок и видать ничего поэтому не отрисовывается
http://localhost:5173/versions/2
Аналогично происходит при открытии задачи 21
Ты это не исправил



Сделай так что бы слева от файла была иконка которая соответсвует формату файла
Добавь красивостей на страницы, состарь ещё дизайн сайта
используй старый бустрап v1

Сделай выбор темы сайта, добавь тёмную, так же сделай выбор языка рус/англ, помни что бекенд работает только с руским

Сделай интерфейс для телефона, что бы по ранзному рисивовались страницы

Как мне сделать так что бы я мог открыть сайт с телефона? ip ноутубка на котором запущен фронтенд 192.168.10.16
при заходе по нему ничего не выдаёт, просто белая страница

Так же сделай логи для фронта, куда он будет записывать с какого ip запрос, что было и тп